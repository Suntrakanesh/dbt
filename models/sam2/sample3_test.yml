version: 2

models:
    - name: sample3
      columns:
        - name: currency
          tests: 
           - accepted_values:
                values:
                    - IN
             